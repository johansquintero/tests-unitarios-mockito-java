package org.mock;

import org.mock.persistence.repository.PlayerRepositoryImpl;
import org.mock.service.PlayerServiceImpl;

//TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or
// click the <icon src="AllIcons.Actions.Execute"/> icon in the gutter.
public class Main {
    public static void main(String[] args) {
//        PlayerServiceImpl playerService = new PlayerServiceImpl(new PlayerRepositoryImpl());
//        System.out.println(playerService.getAll());
//        System.out.println(playerService.getPlayerById(1L));
//
//        playerService.delete(2L);
//        System.out.println(playerService.getAll());

//        Player newPlayer = new Player(7L, "Lamine yamal", "Barcelona", "Delantero");
//        System.out.println(playerService.save(newPlayer));
//        newPlayer.setPosition("Extremo");
//        System.out.println(playerService.update(newPlayer));
//        System.out.println(playerService.getAll());
    }
}